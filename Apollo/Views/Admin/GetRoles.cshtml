
@{
    ViewBag.Title = "GetRoles";
}

<h2 class="raleway">List of Roles</h2>

<table class="table">
<tr>
    <th>
        @Html.Label("Role Name")
    </th>
    @*<th>
        Options
    </th>*@
</tr>

    @foreach (var role in Model)
    {
        <tr>
            <td>
                @role.Name
            </td>
            @*<td>
                @Html.ActionLink("Edit", "EditRole", new { Id = role.Id }, new { @class = "btn btn-info" })
                @Html.ActionLink("Delete", "DeleteRole", new { Id = role.Id }, new { @class = "btn btn-danger" })
            </td>*@
        </tr>
    }
</table>
@Html.ActionLink("Add Role", "CreateRole", "Admin", new { @class="btn btn-primary"})

